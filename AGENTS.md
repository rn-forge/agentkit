# AGENTS.md

This repository keeps a single set of agent instructions, in
[CLAUDE.md](CLAUDE.md).

**Read `CLAUDE.md` now and follow it as if its contents appeared here.** It maps
the repo, states the safety rules for running this tool's own CLI against real
machine config, and lists the conventions you must respect when changing code.
It points at [README.md](README.md) for user-facing detail (install, CLI usage,
configuration model, asset pack) and at
[docs/specs/initial.md](docs/specs/initial.md) for the design record.

Do not add guidance to this file — it would drift out of sync. Put it in
`CLAUDE.md`.
